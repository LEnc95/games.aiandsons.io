name: Policy Release Gate

on:
  push:
    tags:
      - "v*"
      - "release-*"
  workflow_dispatch:

jobs:
  policy-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: "20"

      - name: Validate release checklist + release notes gate
        run: npm run test:policy-gate
